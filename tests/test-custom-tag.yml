name: Test custom tag
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Set up git origin reachable inside the Docker container
        run: |
          git config user.email "test@example.com"
          git config user.name "Test User"
          rm -rf "$GITHUB_WORKSPACE/.git-remote"
          git init --bare "$GITHUB_WORKSPACE/.git-remote"
          git push "$GITHUB_WORKSPACE/.git-remote" HEAD:refs/heads/act-harness
          git remote set-url origin "$GITHUB_WORKSPACE/.git-remote" 2>/dev/null || \
            git remote add origin "$GITHUB_WORKSPACE/.git-remote"

      - id: action
        uses: .
        env:
          CUSTOM_TAG: "my-custom-release-1.2.3"
          DRY_RUN: "true"
          RELEASE_BRANCHES: act-harness,master,main
          BRANCH_HISTORY: last
          GITHUB_TOKEN: ${{ github.token }}

      - name: Assert new_tag equals custom tag
        env:
          NEW_TAG: ${{ steps.action.outputs.new_tag }}
          TAG: ${{ steps.action.outputs.tag }}
        run: |
          echo "new_tag: $NEW_TAG"
          echo "tag: $TAG"
          if [ "$NEW_TAG" != "my-custom-release-1.2.3" ]; then
            echo "FAIL: expected new_tag='my-custom-release-1.2.3', got '$NEW_TAG'"
            exit 1
          fi
          if [ "$TAG" != "my-custom-release-1.2.3" ]; then
            echo "FAIL: expected tag='my-custom-release-1.2.3', got '$TAG'"
            exit 1
          fi
          echo "PASS: custom tag used correctly"
